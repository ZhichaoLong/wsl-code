# 工作日志

## 2026-09-10 — 01：代码核对与目录建立

- 新建 `vem/CurvedVEM_Darcy_gpu`，原项目已有未提交修改，不改原项目文件。
- 原样复制 `mesh/`、`core/gauss_quadrature.*`、`lib/polynomial_basis.*`、`examples/`，用于 CPU 网格准备、原映射与算例对照。GPU 基础矩阵不会由这些 CPU 多项式函数组装。
- 新建 `docs/数学与GPU算法.md`：记录自由度、方向奇偶性、G/B/D/W/H、Piola、方程块、因子型 matrix-free 与硬件分配。
- 已检测 CUDA 13.0，实际 GPU 为 RTX 5060 系列、显存约 8 GiB。沙箱内 GPU 被阻止，使用已授权的沙箱外 GPU 访问测试。
- 阅读 CPU HdivMatrix、DarcySolver、MSSolver 与 H¹ CUDA 参考。注意边界方向必须保持原 CPU 外法向约定；原 MS 稳定化仅在组分对角块。

## 02：基础 CUDA 核心完成并通过第一阶段测试

- `include/device.cuh`：RAII 显存数组、CUDA/cuBLAS 错误检查、计算域缩放基、边迹展开和协作小矩阵消元。
- `include/hdiv_gpu.cuh`、`src/mesh_pack.cpp`：保持原网格局部顺序，生成 ragged 索引/方向/积分几何，按边数分桶；几何只在 CPU 准备一次。
- `src/hdiv_gpu.cu`：GPU 计算 D/G/W/H/H*/B/P，固定几何跨时间步复用。
- `Makefile`：CUDA 13 / sm_120 独立构建，正式程序不依赖 PETSc。
- 临时 `tests/test_stage.cu` 在真实 GPU 检验 square_2x2，k=0..3。max|BD−G|≤6.67e−16，max|PD−I|≤1.67e−13；退出 0。详见 `build/test_stage1.log`。
- 首次编译发现 g++ 不认识设备专用函数体，增加 `__CUDACC__` 守卫后编译与测试通过。
- 按要求删除已通过的本阶段临时测试源码，继续方程层。限制：当前明确支持 k=0..3，k=3 用 12 点三角形规则；不把原 k=4 的积分/基空间边界问题带入 GPU。
- 开始 `include/problem_device.cuh` 和 `include/equation_gpu.cuh`：原 Darcy 1/2、MS 1..4 的设备端数据函数、方程子类与求解接口。

## 03：方程、边界与 GMRES 第二阶段通过

- `src/equation_gpu.cu`：GPU 组分系数、加权 Gram、稳定化、质量矩阵；分片因子型单元算子；边界 lift 与 Q A Q+I−Q；GPU 右端/历史项/误差和质量归约；近似 Schur 对角预条件。
- `src/krylov.cu`：GPU 向量的右预条件重启 GMRES，二次 MGS 正交化，每次接受解前复算真实残差。
- 真 GPU 第二阶段：Darcy 16 单元，39 次线性迭代，真实相对残差 9.82e−11；MS 制造解首步 4 次 Picard，最后真实线性残差 9.98e−11，浓度物理 L² 误差 5.07e−4。记录在 `build/test_stage2.log`。
- 已发现并去掉 matvec 主机端重复扫描全网格取最大边数的开销，改为构造时缓存。
- 删除已通过的第二阶段临时测试源码，下一阶段执行原 CPU 逐矩阵、方向、全局算子与边界右端对照。

## 04：原 CPU 实现交叉验证通过

- 临时 `tests/test_stage.cpp` 直接链接原目录的 HdivMatrix/DarcySolver/MSSolver 与 PETSc，正式 CUDA 程序不链接它们。
- k=0..3 两个不同局部方向单元的 D/G/W/H/B/P 全部对照通过；k=3 投影本身系数较大，绝对差约 4.1e−10，按矩阵最大值缩放后的差约 1.3e−15。
- Darcy 1/2 与 MS 1..4：全局随机向量 matrix-free 结果与原 CPU 稀疏装配 MatMult 一致，最大缩放差约 1.1e−15。受约束算子、非零边矩、lift 后 RHS 均通过。
- 对每个 MS 算例另给非恒定浓度多项式，逐项对照 9 个加权 Gram 与对角稳定化系数；误差处于机器精度。t=0.013 的非零边界通量亦通过。
- `build/test_stage3.log` 保留全部实测数值。删除本阶段测试源码，继续完整 CLI、Euler/BDF2、多步守恒和网格/线程参数覆盖。

## 05：Euler/BDF2 多步和误差对照通过

- `src/main.cpp`：独立 CLI、三步默认 BDF2、Euler 选项、Picard/GMRES 容差、分片与线程参数、JSON 报告、逐步 history.csv 和最终 solution.csv。
- 在同一 16 单元网格上，原 CPU 与新 GPU 分别推进 Euler/BDF2 三步至 t=0.003，逐自由度最终解与原计算域误差对照全部通过，见 `build/test_stage4.log`。
- 初值与 BDF2 第二步中的 c⁰均直接采用积分点初值；每次 Picard 收敛后重新以最终浓度更新算子并核对非线性真实残差。
- GPU 后处理改为 block 内积分点并行、分层归约，避免每组分单线程串行积分。增加 GPU 初始质量积分，供无源零通量算例守恒验收。
- 新增 `mesh/data/hanging_demo.vtk` 与 `triangles_demo.vtk` 两个示例输入，沿用原 VTK 网格读取器，用于变边数/共线悬点/三角形路径。
- 删除已通过的本阶段临时测试源码。接下来优化 GMRES 的正交化调度并执行最终实测；不将小网格上的 GPU 加速视为当然成立。

## 06：全阶次、网格类型与守恒验收；工具限制

- k=0..4 的三角形、共线悬点多边形、普通四边形均通过多项式再现性；tile=1/4096、threads=32/128 算子差异≤2.5e−18。详见 `build/test_stage5.log`。
- k=4 新增 CPU 几何准备阶段的 6×6 Duffy 积分规则（36 点），GPU 标量基自然扩展至 P5；不修改原网格模块。k=4 三角形 PD−I 最大约 3.8e−10，缩放单项式高阶条件数的影响必须保留在验收容差中。
- MS 1/3 三步每组分质量最大漂移分别 1.33e−13、2.96e−12；MS 4 的 Q8 曲边三步、Darcy 网格加密、悬点二阶与四阶完整求解全部收敛。
- 批量 CGS2 与原 MGS2 三步解最大差 1.77e−15，求解用时由约 5.15 s 降至 0.46 s（同一小网格，两次运行实测，非 CPU/GPU 加速比）。并行误差归约差异≤1.5e−15，见 `build/test_conservation_optimized.log`。
- 已增加实际 `cudaFuncGetAttributes` / occupancy 查询，运行日志报告每核寄存器、local memory、shared memory 和每 SM 可驻留 block 上限。
- Compute Sanitizer **未能完成检查，不能写成通过**：Windows WDDM debugger interface 未启用，工具要求管理员运行 EnableDebuggerInterface.bat，返回 86，详见 `build/sanitizer_basis.log`。真实 GPU 普通执行与所有数值断言通过；未更改 Windows 调试/驱动设置。
- 阶段 5 临时测试源已删除。进一步去掉 Picard 内临时显存反复分配；把每时间步源项/真解采样独立为 GPU 核并缓存，减少误差核寄存器压力；正式求解释放 G/B/H/H* 诊断矩阵，仅保留 D/P/W/H⁻¹。

## 07：装配性能对照与线程选择

- 原 CPU 单次完整 MS 装配与 GPU 重新计算基础矩阵+更新系数/预条件+右端对照：16/64/256 单元均通过随机算子验证，缩放差≤7.2e−16。
- 实测 CSV：`build/performance_cpu_gpu.csv`。256 单元 CPU 装配约 895.7 ms，GPU 对应准备约 3.83 ms；这是装配准备阶段比较，不是端到端求解加速比，CPU 1 次、GPU 各阶段 10 次平均，双方均不含首次网格准备。
- 4096 单元测试 threads=32/64/128/256：前三者 matvec 约 0.127–0.130 ms，256 线程约 0.187 ms；保留 128 默认值，较高阶矩阵也有足够线程覆盖。记录在 `output/bench_threads*/`。
- MS 制造解 16/64/256 单元的物理浓度误差呈约二阶下降；全部三步非线性残差约 1e−11。
- 删除阶段性能测试源码。依据测量，继续优化 MS 线性依赖浓度的系数更新：预积分单项式–向量基–几何张量矩，Picard 时做小张量收缩；显存超预算自动回到已验证的逐积分点核。

## 08：MS 预积分收缩完成并验证

- `src/equation_gpu.cu` 新增 `preintegrate_moments` 和 `contract_moments`。固定几何矩 C/Z 在 GPU 构造一次，Picard 用浓度多项式系数精确收缩；初值继续直接积分。256 MiB 附加缓存预算，超额走直接核。
- `include/equation_gpu.cuh` 提供缓存状态与直接路径切换，保留可比较的直接算法；CLI 性能报告新增 `picard_update_ms`。
- 在正弦、半圆环、逐单元 Q8 映射，k=0..4，释放诊断基础矩阵后，对照非恒定浓度、非零随机通量的两条算法路径；加权 Gram、alpha 与全局算子全部通过，见 `build/test_stage7.log`。
- 对测试输入作过修正：最初通量为零会让算子比较跳过通量块，改用非零随机通量后重新通过。
- 删除本阶段临时测试源码；补充 `README.md`、`docs/GPU分配与内存.md`，更新数学文档的完整流程和预积分推导。

## 09：最终重编译、验收与交付

- 最终 `make -B -j4` 完整重编译无 warning/error；重新运行 MS 三步，结果与前一轮一致。欠积分与非法线程数返回 1，并把报告标为 failed，避免留下旧成功状态。
- 公开算子增加组分数/质量系数/输入输出别名检查，防止将不支持配置静默传进 CUDA 核。
- 最终 CPU 参考解对照仍通过：Euler 3.58e−9，BDF2 7.48e−9；MS 1/3 组分质量漂移 1.33e−13、2.36e−12。Darcy 压力加密阶约 2.13/2.07，MS 浓度约 1.94/2.21。
- 正弦/Q8/半圆环、Darcy、MS 四阶均实际运行成功。65536 单元/1969152 自由度完成准备与 matvec 基准，**没有把它描述为完整时间推进**。
- 4096 单元 Picard 收缩+预条件约 0.93 ms；65536 单元约 17.84 ms，matvec 约 2.98 ms。初值仍使用直接积分保证间断初值语义。
- `docs/验收与性能.md` 汇总数值、性能、复现命令和 WDDM sanitizer 限制；`docs/validation/` 保留可版本化的日志、CPU 参考解、JSON/CSV 报告、最终源码/程序哈希。
- 最终临时 `tests/test_stage.py` 断言全部通过并删除；同时清理本任务测试二进制、测试对象与临时编译脚本。`tests/README.md` 说明测试删除约定。正式程序、数学文档、运行示例和完整工作日志保留。

## 10：独立主函数与全部算例入口（2026-09-10，开始）

- `include/solver_driver.h`：提取共享求解参数，声明单算例与全部算例运行接口。
- `src/solver_driver.cpp`：迁移原 main 中的参数处理、初始化、时间推进、Picard、GMRES 调用和结果输出，供两个主函数共用。
- `src/main.cpp`：改为只负责单算例程序入口和命令行异常处理。
- `src/main_all.cpp`：新增独立的全部算例主函数，计划依次计算 Darcy 1/2 和 MS 1/2/3/4，按算例分目录输出。
- 下一步：实现批量调度与汇总、修改构建规则、编写临时测试并运行真实 GPU 求解；此时尚未标记验证通过。

- `src/example_suite.cpp`：实现六算例顺序运行、逐算例释放资源、独立输出、summary.csv 汇总及失败后继续/整体非零退出。
- `Makefile`：构建 `build/vem_gpu` 与 `build/vem_gpu_all`，新增 `make run-all`。
- `README.md`：补充两个主函数的位置、全部算例命令、默认网格和时间参数、输出结构与失败语义。
- 第一轮编译通过；正在验证参数帮助、六算例完整求解、与重构前解的回归比较以及失败汇总。

## 11：全部算例主函数验收通过（2026-09-10）

- `build/vem_gpu_all` 实际计算 Darcy 1/2 与 MS 1/2/3/4，16 单元、k=1，MS 三步 BDF2 至 t=0.003；六个算例全部收敛，最终相对残差均低于 1e-11。输出在 `output/all_examples_20260910/`。
- `build/vem_gpu` 单算例入口重新运行 MS 2 的三步 Euler，收敛且通过重构前解的回归对照，输出在 `output/single_entry_euler_20260910/`。
- 临时 `tests/test_main_suite.cpp` 检查全部六例调度、公共参数透传、模拟单例失败后继续、汇总状态与整体退出码；通过。
- 临时 `tests/test_main_results.py` 检查真实 GPU 输出、时间/历史行数、有限解向量、质量守恒、命令行帮助/非法选项，并与已有 Darcy 1 和全部 MS 的解比较；通过，批量解最大绝对差 2.97e-15。
- 首次普通沙箱运行无法访问 CUDA 驱动，转为获准的沙箱外执行后通过。未将环境失败记作数值验证通过；未重跑受 WDDM 限制的 sanitizer。
- `docs/validation/main_entries_20260910/` 新增运行命令、验收说明、报告快照及测试日志；`docs/验收与性能.md` 与 `tests/README.md` 增加证据入口。
- 按要求删除已通过的两个临时测试源与临时测试二进制，保留测试日志。正式入口不包含测试代码。

## 12：按 CPU 工程风格重构配置与注释（2026-09-10，开始）

- 已阅读原 `main/Darcy_curved_main.cpp`、`main/MS_curved_bdf2_main.cpp` 的集中参数、算例循环与中文注释风格。
- 计划让 `src/main.cpp`、`src/main_all.cpp` 直接配置算例和输出，新增独立性能主函数；原 CLI 保留为单独兼容入口。
- 将参数校验从命令行解析中独立，确保 main 直接配置时仍校验阶次、积分、线程和迭代参数；记录实际参数与输出文件名。
- 为所有工程 C++/CUDA 源码及头文件补充中文文件说明；新 GPU 核、父子类、存储和关键公式增加详细说明。CPU 复制模块保留既有注释并补充模块职责，原 CPU 工程不改。
- 修改前文件哈希保存在 `docs/validation/main_style_20260910/before_sha256.json`；下一步实施并以完整求解回归验证。

- `src/main.cpp`、`src/main_all.cpp`：改为中文分区参数配置，明确各例的方程编号、网格、时间、GPU 参数及输出名称，主函数不再调用 CLI 解析。
- `src/main_benchmark.cpp`：新增源码配置的性能入口；`src/main_cli.cpp`：保留旧命令行功能，单独编译为 `vem_gpu_cli`。
- `include/solver_driver.h`：每个参数独立声明并注释；新增 OutputFiles，支持自定义全部输出名称。`src/solver_options.cpp`：统一配置校验与自动积分规则，保留低阶原有积分规则。
- `src/solver_driver.cpp`：接收 main 配置，保存实际参数快照，按指定文件名输出；`src/example_suite.cpp`：接受显式算例列表、逐例参数和汇总文件名，检查输出重名并正确转义 CSV 路径。
- `Makefile`：四个独立入口共享求解器对象，run/run-all/benchmark 均无运行参数；`.clang-format`：与原工程接近的四空格、分行格式。
- 全部 30 个源码/头文件添加中文模块说明；GPU 基础、方程、设备工具、Krylov、网格打包增加函数与数学/内存注释；CPU 复制模块补充职责说明，未更改计算代码。
- 首轮编译发现基础核文件头中的 H* 与斜杠相邻提前结束块注释，已改为顿号分隔矩阵名，随后编译通过。
- 临时 `tests/test_style_tokens.py` 验证 22 个数学/CPU 文件移除注释和空白后的词法内容完全一致，并检查 30 个代码文件的中文说明；通过。`tests/test_style_config.cpp` 验证直接配置、自动积分、逐例透传、输出重名与失败继续；通过。
- README 与数学文档已切换到 main 配置工作流；正在执行真实 GPU 主函数和自定义文件名回归验证。

## 13：main 配置风格重构验收通过（2026-09-10）

- 四个正式入口均已编译并实际运行：单算例、六算例、4096 单元性能、CLI 的 VTK 悬点二阶算例。
- 六个完整求解全部收敛；与重构前逐自由度对照最大绝对差 7.577e-15，MS 1/2/3/4 四例完全相同；单例 BDF2、另测 Euler 和悬点解回归通过。
- 临时 `tests/test_style_gpu.cpp` 在 C++ 中直接设置自定义输出文件名；先运行故意缺失的网格，再运行 Darcy 和 MS Euler，失败状态、后续继续及真实输出均通过。临时 `tests/test_style_results.py` 检查所有真实结果与参数快照，通过。
- 30 个正式代码文件都有中文模块说明；22 个数学/CPU 文件通过删除注释与空白后的词法一致性验证，确认本次注释与格式整理没有改动计算表达式。
- `docs/validation/main_style_20260910/` 保存最终构建日志、配置/词法/结果测试日志、运行参数、各算例报告、故意失败证据和文件哈希。
- `README.md` 改为 main 内设置→make→无参数运行；`docs/数学与GPU算法.md`、`docs/验收与性能.md`、`tests/README.md` 增加本轮流程及验收入口。
- 通过后删除本阶段四个临时测试源码与两个测试二进制，保留实际输出与测试日志；原 `CurvedVEM_Darcy` 工程未修改。

本轮注释文件与职责补充清单：

| 文件 | 新增说明内容 |
|---|---|
| `include/device.cuh` | 显存所有权、单元偏移/矩阵尺寸、基函数顺序、边迹系数和共享内存求逆 |
| `include/hdiv_gpu.cuh` | CPU 打包结构、Hdiv 基础父类、矩阵缓存及资源接口 |
| `include/equation_gpu.cuh` | 方程视图、Darcy/MS 子类、求解/边界/误差接口和 GMRES 向量职责 |
| `include/problem_device.cuh` | 三组分系数、制造解/间断初值、通量与源项定义 |
| `src/hdiv_gpu.cu` | G/H/H*/D/W/B/P 的计算含义与 block 分工 |
| `src/equation_gpu.cu` | 方程积分/预积分、方向 gather/scatter、边界 lift、时间历史和误差归约 |
| `src/krylov.cu` | GPU Arnoldi/CGS2、CPU Givens/回代及真实残差验证 |
| `src/mesh_pack.cpp` | CPU 静态几何、单元分桶、局部编号以及偶数边矩变号原因 |
| `core/gauss_quadrature.h`, `core/gauss_quadrature.cpp` | CPU 积分规则与静态几何准备职责 |
| `mesh/straight_mesh.h`, `mesh/straight_mesh.cpp` | 原网格数据、逆时针拓扑、全局边与 GPU 打包的关系 |
| `mesh/curved_mesh.h`, `mesh/curved_mesh.cpp` | 原 CPU 曲边数据及操作职责 |
| `mesh/polygon_triangulator.h`, `mesh/polygon_triangulator.cpp` | CPU 多边形剖分及体积分区域生成 |
| `lib/polynomial_basis.h`, `lib/polynomial_basis.cpp` | CPU 缩放基/梯度/补空间与设备基函数对应关系 |
| `examples/darcy_problem.h`, `examples/darcy_problem.cpp` | Darcy 算例/几何的 CPU 初始化与设备数据对应关系 |
| `examples/ms_problem.h`, `examples/ms_problem.cpp` | MS 四算例及 CPU 逐单元映射与设备数据对应关系 |

## 14：按原 main 组织独立算例与收敛实验（开始）

- 已核对原 Darcy_curved、MS_curved_bdf2、MS_triblock_bdf2 的网格循环、dt(h)、共同终止时间与累计误差表；同时核对半圆环/间断初值、固定步长制造解和性能入口。
- 原 BDF2 使用 dt_target=0.1*h^((k+1)/2)，向上选 2^a*5^b 形式的步数后令 dt=T/N；新实现将避免原 100000 步表耗尽时静默超过目标步长。Euler 配套 dt_target=C*h^(k+1)。
- 将四个已有主函数移到 `main/*_main.cpp`；计划按 Darcy 1/2、MS 1/2/3/4 新增独立入口，每次只运行选定算例的网格序列。
- 计划添加求解结果的 C++ 返回结构、公共网格尺度/时间网格/收敛表工具，主函数显式指定参数、网格列表和输出文件。
- 尚未完成构建/数值验证，完成后追加实际结果。

- `main/Darcy_curved_main.cpp`、`Darcy_straight_main.cpp`：分别运行 Darcy 1/2 的网格序列。
- `main/MS_curved_bdf2_main.cpp`、`MS_triblock_bdf2_main.cpp`：分别运行 MS 2/4，显式设置共同 T、C、k、网格序列、范数和输出名称。
- `main/MS_discontinuous_main.cpp`、`MS_half_annulus_main.cpp`：分别运行 MS 1/3 的扩散和质量守恒检查，默认单网格长时间过程。
- 原四入口迁为 `main/vem_gpu*_main.cpp`，src 内已没有 main；Makefile 自动发现主函数，make 名字只编译，默认 run 为单个 MS 2 实验。
- `include/solver_result.h`、`src/solver_result.cpp`：结构化返回 CPU 小型误差/计时结果，并打印各组分最终指标；ErrorReport 从方程头迁入公共结果头。
- `include/mesh_study.h`、`src/mesh_study.cpp`：实际网格尺度、Euler/BDF2 dt(h)、平滑步数与上限校验、网格复用、误差阶公式、逐层 CSV/终端表和失败保留。
- `src/solver_driver.cpp`、`include/solver_driver.h`、`src/solver_options.cpp`：可关闭最终解保存、选择诊断/终端间隔；分开记录迭代/误差/保存耗时；复用已读取的网格。
- `README.md`、`main/README.md`：介绍独立入口、同一 T、计算域/物理域范数及输出文件；默认不批量运行全部方程算例。
- 构建通过。临时 `test_study_cpu.cpp` 验证 k=0..4 的时间步指数、共同终点、步数上限、零/负阶及后层失败保留；通过。独立 Darcy 2 主函数四层全部通过，最细层压力阶 2.048、通量阶 2.021。正在验证 MS。

## 15：独立主函数与收敛实验验收完成

- `build/Darcy_straight` 正式主函数默认四层全部收敛，最细层压力/通量阶分别 2.047901/2.021224，累计终端输出与 CSV 的阶通过独立复算。
- 临时 `tests/test_study_gpu.cpp`：MS 2/4 在 16、64 单元上使用 dt_target=0.1h，分别 4×0.025 和 8×0.0125，均到 T=0.1；三组分原始误差与实测阶输出正确。MS 4 的通量粗网格阶约 0.99–1.12，照实保留。
- MS 1/3 用 T=0.003 做守恒回归，解与旧记录一致；不是默认 T=0.5 的完整长时间运行。MS 2/4 默认更细的 256/1024 单元本轮未完整推进，未把小网格验证写成全规模完成。
- `tests/test_study_diagnostics.cpp`：新增的中间步所有组分误差/质量与耗时输出通过短 GPU 回归；仍与原三步 BDF2 解完全一致。
- `tests/test_study_results.py`：独立复算阶、检查共同 T、时间步尺度、诊断输出行数、save_solution=false、计时、无真解质量及旧解，全部通过。
- 最终构建通过，无 warning/error；GPU 数学核/GMRES 方法未修改，公共结果结构只迁移定义。`main/` 中共有 10 个独立入口（6 个按方程命名，4 个保留兼容），src 不再含 main。
- `docs/validation/independent_main_20260910/` 保留正式运行、主机测试、GPU 两层实验、诊断与结果断言日志，及输出/参数快照。README、main/README 与数学/验收文档同步更新。
- 按要求删除通过的四个临时测试源及测试二进制，保留日志；原 CPU 工程未改动。

## 16：MS 4、k=1、T=0.1 六层实际运行（2026-09-10）

- `main/MS_triblock_bdf2_main.cpp`：按最新要求将网格编号改为 `{2,4,8,16,32,64}`；保留用户设置的 Picard 容差 1e-6、线性容差 1e-11、BDF2 和 dt_target=0.1h。
- 网格文件编号 n 对应实际 (2n)×(2n) 单元；因此本次实际为 4×4 到 128×128，包括 64×64。遵循显式编号列表，结果按实际单元数标注。
- 输出为 `output/MS_triblock_bdf2_k1/`；本节开始时尚未运行，后续追加实际成功/失败与验证记录。

- 首轮实际 4×4、8×8、16×16 完成，耗时 6.920/27.061/101.078 s；32×32 首次线性求解达到 4000 次上限。首轮输出保留在 `output/MS_triblock_bdf2_k1_restart80_failed/`。
- 主函数 GMRES restart 从 80 改为 320、maxit 从 4000 改为 20000，容差不变，重新运行全部六层以保持同一配置；`src/krylov.cu` 将失败残差改为科学计数法输出。
- restart=320 重跑前三层完成，耗时 4.551/17.900/69.553 s；与首轮各组分标量/通量误差相对差均小于 1e-7。32×32 已通过原首步失败点，继续推进。临时 `tests/test_ms_requested_run.py` 将独立验证六层 18 行数据、各 Picard 线性残差、终点和收敛阶，尚待全程结束。
- 用户再次确认完整列表，必须完成实际 128×128。restart=320 的 32×32 已完成 T=0.1，总耗时 404.211 s，线性迭代累计 394063；三组分标量阶 2.260/2.303/2.251，通量阶 2.106/1.843/1.857。64×64 首步成功，耗时 46.540 s。
- `output/MS_triblock_bdf2_k1/结果汇总.md`：先保存明确标注“运行中”的前四层表；全部结束后更新，未完成层不填写误差。64×64 已完成 26/64 步；已检查的 126 次 Picard 线性求解残差均 ≤1e-11，单次最多 6434 次迭代。
- 64×64 已完成 64 步到 T=0.1，总耗时 3788.589 s，累计线性迭代 1067248。三组分标量阶 2.198/2.203/2.136、通量阶 2.150/2.129/2.048；运行中汇总更新为五层。128×128 已开始（16384 单元、493056 自由度、128 步）。
- 128×128 首步成功：264.917 s，3 次 Picard、累计 20093 次线性迭代、最终非线性相对残差 3.715e-9。GPU 显存约 2820 MiB，利用率约 96%；后续继续完整 128 步，不把首步当全程结果。
- 128×128 已完成 10/128 步，累计 2417.771 s，当前每步约 267 s，真实线性残差仍 ≤1e-11；完整终点尚未完成。
- 128×128 已完成 20/128 步到 t=0.015625，累计 5110.994 s，最新一步 254.295 s、非线性残差 3.099e-7。仍在继续推进，最终误差未计算。

2026-09-10T13:05:28：用户要求停止，已发送 SIGTERM，进程退出码 143，并确认进程已退出。128×128 日志最后完整记录为 25/128 步（t=0.01953125）；未完成 T=0.1，无最终误差，未保存可续算检查点。前五层结果保留。

## 17：PETSc GPU 代数层重构（开始）

- 用户要求矩阵、向量、求解器迁至 PETSc，逐功能验证及内存释放验证；不恢复已停止的长时间六层任务。
- 确认 /home/lzccode/petsc/arch-linux-c-opt 为 CUDA、双精度实数构建。保留旧二进制及关键源码快照于 build/vem_gpu_cli_before_petsc 与 docs/validation/petsc_refactor_20260910，供回归。
- 计划：RAII 对象与配对 GPU 访问 → PETSc Vec/KSP → GPU COO 稀疏 Mat 与预条件 → 基础矩阵 PETSc 存储 → 全算例回归、算子对照与重复创建释放检查。
- `include/petsc_gpu.h`：PETSc 会话、Mat/KSP/IS 唯一所有权、Vec/批量 Dense CUDA 数组借用恢复；main 全入口先创建会话、最后 Finalize。`tests/test_petsc_storage.cpp` 实际 GPU 50 次创建/读写/释放通过，已删除测试源，保留 storage_test.log。
- `Makefile` 接入已安装 PETSc 与 MPI 编译器；`src/krylov.cu` 替换自写 GMRES/cuBLAS 代数为 PETSc KSP/Vec；`src/equation_gpu.cu` 增加 GPU COO 单元条目与一次稀疏图预分配。正在进行算子与求解回归。
- 阶段算子测试通过：k=0..4、Darcy/MS、四边形/三角形/悬挂边、两次系数更新；28 组稀疏 Mat 与 matrix-free 作用相对差最大约 4e-15，P*D 最大误差 5.3e-12。六算例三步/稳态和 PETSc MatShell 求解全部通过。
- 六算例与旧实现保存的同参数误差/质量逐项对照通过；MS1/3 质量守恒通过。测试源已删除，日志归档 operator_six_examples.log、regression_comparison.log。默认自然排序 ILU(0) 已使用 cuSPARSE 的 GPU 数值分解路径；后续验证内存与尺度性能。
- 生命周期/内存验收通过：64 单元，24 轮创建，每轮 4 次系数/矩阵/KSP 更新及误差积分。预热后 PETSc 跟踪主机分配固定为 55184 字节，GPU 空闲显存固定为 7297040384 字节；每轮无遗留 Mat/Vec/KSP/PC/IS，Finalize 无未释放内存报告。借用数组时抛异常、派生构造失败及故意 KSP 不收敛的清理均通过。临时测试源删除，memory_lifecycle.log 与对象快照归档。
- 细网格验证发现自然排序 ILU(0) 在实际 64×64、RCM + GMRES 在 128×128 重启时失败，均保留失败证据，没有放宽真实残差门限。改测 PETSc FGMRES 与更强预条件；早期小网格测试不代表大网格性能验收。
- `src/krylov.cu`：Restore 双数组即使其中一次出错也继续归还；切换方程时销毁旧 VecScatter。`include/device.cuh` 补新缓冲区异常回收；`include/petsc_gpu.h` 补会话错误及存储构造异常清理。兼容 CLI 的帮助路径改为正常栈展开，避免 std::exit 跳过 PetscFinalize。
- 已安装 PETSc 的 GPU ILU(0) 数值核忽略 MatFactorInfo 的 shift；仅在支持移位的 CPU 数值分解路径启用该选项，文档和实际后端日志必须据此说明。
- `src/krylov.cu` 改用 PETSc FGMRES，增加最多两次严格 `Aδ=b−Ax` 残差校正；校正计入 maxit，仍以原顺序真实残差验收。自然排序/RCM 的细网格失败及 ILU(1) 失败保留证据。
- 细网格性能验收通过：MS4 k=1 实际 32/64/128 首步分别 3.614/20.339/135.569 s；旧版同参数首步约 9.081/46.540/264.917 s，比值 2.51/2.29/1.95。仅为首步样本，不是完整 T=0.1；没有恢复已停止的长跑。
- 默认排序按方程选择：Darcy natural（先通量后压力/均值），MS ND（嵌套剖分提高三角求解并行度）。ND+Darcy 零主元测试揭示两种混合块不应共用相同排序。`SolverOptions` 的 auto 在校验时解析并记录实际选择，各命名主函数显式设置。
- 最终 32 组投影/COO/MatShell/置换对照通过：矩阵作用相对差 ≤4.11e-15，置换 ≤4.14e-15，PD−I ≤5.23e-12。六算例 BDF2/稳态、MS4 Euler/MatShell 以及 k=4 Darcy/MS 全部通过，与原误差和质量对照通过。115 次真实线性残差均 <1e-11。
- 最终排序路径内存验收通过：4 轮预热后 24 轮、每轮 4 次更新/求解，PETSc 跟踪主机分配恒定 54192 B，CUDA 空闲显存恒定 7301234688 B。对象检测器用活 Vec 作正控制；正常/借用异常/构造失败/KSP 失败/不同算子切换均无代数对象遗留，Finalize 无未释放报告。证据 memory_final.log、final_objects_*.log。
- 文件职责补充：`include/hdiv_gpu.cuh` 的基础矩阵用 PetscDense；`include/equation_gpu.cuh` 的方程矩阵/向量及稀疏图拥有权用 PETSc；`src/solver_driver.cpp` 改 PETSc Vec 历史/范数并输出 PETSc 配置、COO 字节与 CSR 非零数；`src/solver_options.cpp` 增加主函数与 CLI 共用配置校验；`main/` 10 个入口管理 PetscSession。`Makefile` 使用 MPI/PETSc，删除应用直接链接 cuBLAS，修复临时测试依赖。原 CPU 工程未被本轮修改。
- 文档同步：README、main/README、PETSc_GPU重构、GPU分配与内存、数学说明和验收文档更新。通过的临时阶段测试源码删除，数值/内存/失败与性能证据归档 `docs/validation/petsc_refactor_20260910/`。全部主函数已完成编译。
- 最终 CLI 冒烟运行通过：MS4 三步、实际 Vec/Mat/KSP 类型输出、新 JSON 中 COO/CSR 字段及正常退出检查正确；兼容 --help 正常栈展开退出。日志 cli_help_final.log、cli_smoke_final.log；最终源码 SHA256 保存为 source_sha256_final.txt。当前没有本轮测试或旧长跑仍在执行。

## 18：PETSc MS4 k=1 完整六层实验与内存监控

- 用户要求只跑 k=1、网格编号 {2,4,8,16,32,64}；不运行 k=2。主函数保留 T=0.1、dt_target=0.1h、Picard 1e-6、线性 1e-11、ND/ILU(0)、restart=320，以及用户现有 maxit=200000。
- `main/MS_triblock_bdf2_main.cpp` 输出改为新目录 `output/MS_triblock_bdf2_petsc_full_20260910_155857_k1`，避免覆盖旧结果。实际网格 4×4..128×128。
- `src/krylov.cu`、`include/equation_gpu.cuh` 增加 GPU 同步后的 KSPSolve 计时（含 PC 准备与校正）。`src/solver_driver.cpp` 区分 KSP 和整个线性流程时间，每步记录 RSS/显存；`include/solver_result.h`、`src/solver_result.cpp`、`src/mesh_study.cpp` 增加平均每步与 KSP 汇总列。
- 新增 `include/runtime_metrics.h`、`src/runtime_metrics.cpp`：读取进程 RSS 与 cudaMemGetInfo，每步结束及每网格释放后采样。新增 `tools/monitor_run.py`：外部每 5 秒采样 RSS/系统内存/设备总显存，保留时间戳、网格标签与完整运行日志。显存为设备级口径，不冒充进程独占。
- 两层三步临时验证通过：KSP/整体计时分项与逐步求和一致，CSV 无错列，内存读数有效，监控器正常退出，数值误差不变。临时测试源和构建产物已删除，日志在 `docs/validation/ms_k1_full_20260910/`。
- 正式运行于 2026-09-10 16:05:58 +08:00 启动：监控 PID=8275，求解 PID=8283。完整 stdout 和 5 秒采样在新输出目录 monitor/；每步和每网格释放后的采样另写 iterations.log/results.txt。此刻尚未完成全部网格，不预填最终结果。
- 运行中新增 `tools/summarize_ms_study.py`：从已完成的 convergence.csv 与逐步日志生成中文汇总、计时/内存 CSV 和标准 matplotlib 曲线；未完成网格不填写最终误差。临时 `tests/test_full_k1.py` 逐层检查自由度公式、终点、步数、计时和、重新计算的收敛阶与每次真实线性残差；最后全部通过后删除。
- 当前前四层完成；32×32 第 8 步由于首次越过 restart 扩充 Krylov 工作区，显存随后稳定。RSS 后期有增长，需要继续观察，暂不宣称完整长跑无增长。PETSc 源码 cupmcontext.hpp 将 CUDA pool ReleaseThreshold 设为 UINT64_MAX，所以不同网格释放后显存仍可保留池缓存；该机制不单独证明 RSS 增长原因。
- 2026-09-10 约 16:43:04 +08:00：用户因内存增长要求停止。向监控 PID 8275 发送 SIGTERM，其终止求解 PID 8283；已通过 ps 确认两进程均退出。monitor/summary.json 记录 returncode=-15、elapsed_s=2224.991。前五层（实际 4×4..64×64）完成至 T=0.1；128×128 最后完整时间步为 2/128，未完成整层。刷新运行汇总、逐步 CSV 与内存图，保留全部原始证据及未完成验收测试，不运行 k=2。
- 长跑主机 RSS 确有持续增长：64×64 约从 1140 MiB 增至超过 1600 MiB，而该层 CUDA 显存读数基本稳定；本次总峰值 RSS 为 3210.105 MiB。停止后整卡显存从运行峰值 7593 MiB 回落至 1546 MiB（启动前 1545 MiB）。此前短期创建/释放测试不能作为本次长跑无泄漏的结论；尚未定位增长根因，本次未修改求解代码或宣称修复。

## 19：规避 cuSPARSE 重复分解/SpSV 路径的主机内存增长

- 对照原 CPU 工程 `core/petsc_utils.cpp:570` 的 CPU 三角回代规避方式。当前 GPU 工程使用 natural 内部排序和 cuSPARSE ILU(0) 专用数值分解；安装的 PETSc `aijcusparse.cu` 中该分支未检查 `use_cpu_solve`，不能仅复制一个 MatCUSPARSESetUseCPUSolve 调用。
- 计划在 `src/krylov.cu` 明确选择 MATSOLVERPETSC，使因子为 CPU MatSeqAIJ；全局 GPU Mat/Vec、装配、排序与真实残差流程保持现有实现。数值更新由 PETSc MatSeqAIJGetArrayRead 同步到主机，因子由 PC/KSP 持有并释放。
- 新建临时 `tests/test_stage.cpp`：同一方程/KSP 连续 160 次系数更新与求解，逐次记录解误差、真实残差、RSS、CUDA 显存及 PETSc 跟踪分配；另包含六算例回归与 MS4 16×16 的 T=0.1 完整推进。先保存旧后端基线，再验证修改。证据存于 `docs/validation/cpu_factor_20260910/`；不恢复用户停止的六层长跑。
- 已完成 CPU 因子切换和实际类型检查。160 次固定 KSP 对照：预热后原后端 RSS 增长 26.141 MiB，新后端增长 0.043 MiB，显存均稳定；六例全局解最大差 1.679e-13。MS4 16×16 至 T=0.1 误差一致，第 4 至 16 步 RSS 增长 4 KiB。可选 LU/ILU(1) 回归通过。全部主函数编译通过。更新 README、PETSc_GPU重构、GPU分配与内存、solver_driver.h/main 注释；证据目录 README 汇总结果和局限，已通过临时测试及其构建产物删除。

## 20：清理输出并重跑 CPU 因子版本六层实验

- 用户要求清理混杂输出并重新运行当前 MS_triblock_bdf2 主函数，继续监控内存/显存、误差/阶、自由度与耗时。
- `output/` 的 39 个历史测试目录在逐文件 SHA256 校验压缩归档后删除；381 个原始文件及清单在 `docs/validation/output_cleanup_20260910/`。4 个正式/失败/停止实验移至 `output/历史结果/`，包括原 cuSPARSE 长跑增长证据。新增 `output/README.md` 说明目录用途；后续测试仅写 docs/validation。
- `main/MS_triblock_bdf2_main.cpp` 当前输出设置为 `output/MS_triblock_bdf2_k1`，k=1、编号 {2,4,8,16,32,64}（实际 4²..128²）、T=0.1、dt=0.1h、ND/CPU ILU(0)、restart=320、maxit=200000、原容差保留。error_interval 从 0 改为 10，每 10 步及终点保存误差；各步仍记录计时与内存。另每 5 秒监控 RSS/NVML 整卡显存，显存不是独占进程口径。
- 2026-09-10 16:57:31 +08:00 正式启动，监控 PID=23901、求解 PID=23915。保存 main 源码快照及 main/krylov/二进制 SHA256。`tests/test_full_k1.py` 增加 --root 参数，适配新目录并可显式检查已归档旧结果；待全部六层完成再运行 --final，不提前宣称通过。
- 运行中前四层已完成并通过自由度、时间终点、误差阶、计时求和、全部线性真实残差检查；与旧正式运行的误差对照一致。32×32 平均每步 4.1166 s、KSP 129.8584 s、RSS 704.457 MiB 恒定。64×64 已完成第 35 步，RSS 1551.676 MiB、cudaMemGetInfo 显存使用 2465.563 MiB 在全部已完成步末恒定。后两层尚未全部完成，不能作最终无增长结论。
- `tools/summarize_ms_study.py` 增加更新时间和从真实日志提取的代数后端，区分历史 GPU 因子和当前 CPU 因子；运行中定期刷新汇总与内存曲线。
