# =========================================================================
# CurvedVEM_Darcy 专用忽略规则
#
# 注意：仓库根 .gitignore 有 4 条规则会误伤本项目源码，必须在此逐条恢复。
# 根目录那条 "!Makefile  # 但项目自己写的 Makefile 要保留！" 之所以没起作用，
# 是因为 gitignore 不支持行尾注释——整行会被当成一个字面模式，
# 那条负向规则实际从未生效（根 .gitignore 第 32 行 "*.d  # ..." 同理）。
# 所以本文件里所有注释都单独占一行。
# =========================================================================

# ---------- 必须排除 ----------

# 算例输出，约 20 GB，不上传。加前导 / 限定本目录，以免误伤 mesh/data
/data/
# 编译产物
/build/
# 编译好的可执行文件（无扩展名，根规则的 *.out/*.exe 抓不到）
/test_ms_problem
# WSL 从 Windows 拷文件时留下的元数据垃圾
*:Zone.Identifier
# 文档备份，内容与正式版重复
*.md.bak
# 工具配置，不属于项目源码
.claude/

# ---------- 恢复被根 .gitignore 误伤的内容 ----------

# 根 L103 `*.cpp`（本意是 nvcc 的 .cudafe1.cpp 之类中间文件）吞掉了全部 C++ 源码。
# 本项目不用 nvcc 生成 .cpp（已确认无 *cudafe* 文件），全部恢复。
!*.cpp

# 根 L41 `core*`（本意是 core dump）把整个 core/ 目录一起排除了。
!core/
!core/**

# 根 L28 `Makefile` 把手写的 Makefile 排除了。
!/Makefile

# 根 L47 `data/` 连运行必需的网格文件一起排除了。
# 目录要先解除排除，git 才会进去扫里面的文件。
!mesh/data/
!mesh/data/*.msh
